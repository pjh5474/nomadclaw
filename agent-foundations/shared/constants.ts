/** 기본 채팅방 Agent instance name */
export const DEFAULT_ROOM_ID = "default";

/** 채팅방 기본 표시 이름 */
export const DEFAULT_ROOM_NAME = "채팅방";

/** 관전자 기본 닉네임 */
export const SPECTATOR_NICKNAME = "관전자";

/** 투표 선택지 최소 개수 */
export const MIN_VOTE_OPTIONS = 2;

/** 마감 연장 단위 / 마감 임박 기준 (5분) */
export const VOTE_EXTEND_MS = 5 * 60 * 1000;

/** 투표 생성 폼 기본 마감 (30분 후) */
export const DEFAULT_VOTE_DURATION_MS = 30 * 60 * 1000;
